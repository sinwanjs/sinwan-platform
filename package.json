{
  "name": "sinwan",
  "version": "0.1.0",
  "description": "A modern web framework for building fast and scalable web applications.",
  "scripts": {
    "build:core": "bun run build -w @sinwan/core",
    "build:meta": "bun run build -w @sinwan/meta",
    "build:logger": "bun run build -w @sinwan/logger",
    "build": "bun run build:core && bun run build:meta && bun run build:logger",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ]
}
