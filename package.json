{
  "name": "sinwan",
  "version": "0.1.0",
  "description": "A modern web framework for building fast and scalable web applications.",
  "scripts": {
    "build:core": "cd packages/core && bun run build",
    "build:meta": "cd packages/meta && bun run build",
    "build:logger": "cd packages/logger && bun run build",
    "build": "bun run build:core && bun run build:meta && bun run build:logger",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "workspaces": [
    "packages/*"
  ]
}
